# Gustavo app config for Qwen36-35B-A3B, an analog of the "qwen3.6-35b-a3b-fp8-dflash.yaml" recipe from
# https://github.com/eugr/spark-vllm-docker/blob/main/recipes/qwen3.6-35b-a3b-fp8-dflash.yaml
#
# vLLM serving Qwen3.6-35B-A3B-FP8
#
# Before uploading:
#  1. Fetch the model weights yourself onto the target spark - Gustavo does not download them.
#     Model: Qwen/Qwen3.6-35B-A3B-FP8
#     Stage them at: /home/ubuntu/cypress-ai/models/qwen-qwen3-6-35b-a3b-fp8
#     e.g. huggingface-cli download Qwen/Qwen3.6-35B-A3B-FP8 --local-dir
#     /home/ubuntu/cypress-ai/models/qwen-qwen3-6-35b-a3b-fp8
#  2. GPU access (GPU_ENABLED=true) must already be set on the target spark's worker - this is
#     per-worker, not part of this file. See docs/llm-deployments.md.
#  3. Requires an extra file from the upstream recipe's mod (mods/fix-qwen3.6-chat-template):
#     copy `qwen3.6-fixed_chat_template.jinja` (shipped alongside this file, in
#     `../support-files/`) onto the spark at:
#     /home/ubuntu/cypress-ai/models/qwen-qwen3-6-35b-a3b-fp8/qwen3.6-fixed_chat_template.jinja
#     It's bind-mounted into the container at /workspace/vllm/fixed_chat_template.jinja, which
#     the command below already references.
#  4. This container has no default entrypoint - the full `vllm serve ...` invocation is the
#     `command` list below, matching how https://github.com/eugr/spark-vllm-docker runs it
#     (their containers don't bake in a `vllm serve` default either).
#  5. `docker_image` below (eugr/spark-vllm:latest) is a real, public, arm64-only image built
#     for the DGX Spark's GB10 chip by the recipe repo's author - NOT vllm/vllm-openai. It's a
#     rolling tag (rebuilt nightly); pin a `nightly-YYYYMMDD` tag from
#     https://hub.docker.com/r/eugr/spark-vllm/tags if you need reproducibility.
#  6. tensor_parallel=2 in this recipe's own defaults - this assumes the target spark's worker
#     actually has that many GPUs visible to Docker. Verify against your own hardware before
#     deploying; lower it to 1 if unsure.
#  7. The upstream tool runs these containers privileged by default (for RDMA/InfiniBand and
#     direct hardware access) - privileged: true below matches that. Try privileged: false if
#     your spark doesn't need it.
#  8. This recipe references a second (speculative/draft) model by HuggingFace ID directly
#     inside a flag, not redirected to a local mount like the main model:
#     z-lab/Qwen3.6-35B-A3B-DFlash. vLLM will try to download it over the network at startup
#     unless the container has internet access (and, if the model is gated, a valid HF_TOKEN env
#     var) - it is NOT pre-staged like the main model weights.

docker_image: eugr/spark-vllm:latest

running: true
privileged: true
network_mode: bridge

starting_ports:
  - 8000: 8000

volumes:
  - /home/ubuntu/cypress-ai/models/qwen-qwen3-6-35b-a3b-fp8:/model
  - /home/ubuntu/cypress-ai/models/qwen-qwen3-6-35b-a3b-fp8/qwen3.6-fixed_chat_template.jinja:/workspace/vllm/fixed_chat_template.jinja

env_vars:
  VLLM_MARLIN_USE_ATOMIC_ADD: "1"

command:
  - "vllm"
  - "serve"
  - "/model"
  - "--served-model-name"
  - "qwen36-35b-a3b"
  - "--host"
  - "0.0.0.0"
  - "--port"
  - "8000"
  - "--max-model-len"
  - "262144"
  - "--max-num-batched-tokens"
  - "16384"
  - "--gpu-memory-utilization"
  - "0.8"
  - "--enable-auto-tool-choice"
  - "--tool-call-parser"
  - "qwen3_xml"
  - "--reasoning-parser"
  - "qwen3"
  - "--load-format"
  - "fastsafetensors"
  - "--attention-backend"
  - "flash_attn"
  - "--chat-template"
  - "fixed_chat_template.jinja"
  - "--speculative-config"
  - "{\"method\": \"dflash\", \"model\": \"z-lab/Qwen3.6-35B-A3B-DFlash\", \"num_speculative_tokens\": 15}"
  - "-tp"
  - "2"
  - "--distributed-executor-backend"
  - "ray"

shm_size: "16g"
